import Hero from '@/components/hero';

const Umum = () => {
    return (
        <main>
            <div className="flex min-h-screen flex-col items-center justify-center p-2"></div>
        </main>
    );
};

export default Umum;
